export default function PointsSettlementPreview({ settlement, pointDelta, profile, open, setOpen }) { const rows = [["学习入账", settlement.studyCredit], ["运动入账", settlement.exerciseCredit], ["时间价值转分", settlement.bankPointsAdded], ["睡眠积分", settlement.sleepAdjustmentPoints], ["运动额外积分", settlement.exerciseBonusPoints], ["工作积分", settlement.workPoints], ["日型额外奖励", settlement.dayTypeBonusPoints], ["自由娱乐积分", settlement.entertainmentScoreDelta], ["复盘归档奖励", settlement.reviewTimelinessBonus]]; return <section className="points-settlement-preview"><h2>结算预览</h2><strong>预计 {pointDelta >= 0 ? "+" : ""}{pointDelta} 分</strong><span>保存后余额 {Number(profile?.points || 0) + pointDelta} 分 · 学习 {settlement.studyMinutes}min · 运动 {settlement.exerciseMinutes}min · 工作 {settlement.workMinutes}min · 娱乐 {settlement.totalEntertainmentMinutes}/90min</span><button type="button" onClick={() => setOpen(!open)}>查看积分明细</button>{open && <ul className="points-detail">{rows.map(([label, amount]) => <li key={label}>{label}<b>{amount >= 0 ? "+" : ""}{amount}</b></li>)}</ul>}</section>; }
