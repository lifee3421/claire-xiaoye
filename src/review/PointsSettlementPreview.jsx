export default function PointsSettlementPreview({ settlement, open }) { const rows = [["学习入账", settlement.studyCredit], ["运动入账", settlement.exerciseCredit], ["时间价值转分", settlement.bankPointsAdded], ["睡眠积分", settlement.sleepAdjustmentPoints], ["运动额外积分", settlement.exerciseBonusPoints], ["工作积分", settlement.workPoints], ["日型额外奖励", settlement.dayTypeBonusPoints], ["自由娱乐积分", settlement.entertainmentScoreDelta], ["复盘归档奖励", settlement.reviewTimelinessBonus]]; if (!open) return null; return <div className="points-settlement-detail"><ul className="points-detail">{rows.map(([label, amount]) => <li key={label}>{label}<b>{amount >= 0 ? "+" : ""}{amount}</b></li>)}</ul></div>; }
