import ReviewField from "./ReviewField.jsx";
function Card({ group, draft, onChange, onRestore, onRemove }) { return <section className="review-section-card"><div className="review-card-head"><h3>{group.title}</h3>{group.temporaryId && <button type="button" onClick={() => onRemove(group.temporaryId)}>删除当天项目</button>}</div><div className="review-field-grid">{group.fields.map((field) => <ReviewField key={field.id} field={field} state={draft.fields[field.id]} onChange={onChange} onRestore={onRestore} />)}</div></section>; }
export function ReviewCategoryColumn({ sections, draft, onChange, onRestore, onRemoveProject, onAddProject }) { return <div>{sections.map((section) => <section key={section.title} className="review-category"><div className="review-card-head"><h2>{section.title}</h2>{section.title === "项目" && <button type="button" onClick={onAddProject}>新增当天项目</button>}</div>{section.groups.map((group) => <Card key={`${group.title}-${group.temporaryId || "default"}`} group={group} draft={draft} onChange={onChange} onRestore={onRestore} onRemove={onRemoveProject} />)}</section>)}</div>; }
export function ReviewOtherColumn({ sections, draft, onChange, onRestore }) { return <div>{sections.map((group) => <Card key={group.title} group={group} draft={draft} onChange={onChange} onRestore={onRestore} onRemove={() => {}} />)}</div>; }
