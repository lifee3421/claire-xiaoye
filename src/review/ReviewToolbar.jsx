import { ChevronLeft, ChevronRight, Download, Save } from "lucide-react";
export default function ReviewToolbar({ date, status, saving, onDate, onExport, onSubmit }) { const today = new Date().toLocaleDateString("en-CA", { timeZone: "Asia/Shanghai" }); const shift = (amount) => { const next = new Date(`${date}T12:00:00`); next.setDate(next.getDate() + amount); onDate(next.toISOString().slice(0, 10)); }; return <header className="review-toolbar"><div><p className="eyebrow">Daily review</p><h2>今日复盘与结算</h2><p>系统先整理事实，你只负责修改和补充。</p></div><div className="review-date-controls"><button type="button" onClick={() => shift(-1)}><ChevronLeft size={16} />前一天</button><strong>{date}</strong><input aria-label="复盘日期" type="date" value={date} onChange={(event) => onDate(event.target.value)} /><button type="button" onClick={() => shift(1)}>后一天<ChevronRight size={16} /></button><button type="button" onClick={() => onDate(today)}>回到今天</button></div><div className="review-toolbar-actions"><span>{status}</span><button type="button" onClick={onExport}><Download size={16} />导出 Markdown</button><button className="primary-button" disabled={saving} type="button" onClick={onSubmit}><Save size={16} />{saving ? "保存中…" : "保存复盘并结算"}</button></div></header>; }
