export default function PointsSettlementBar({ pointDelta, profile, saving, onSubmit, revision }) { return <section className="points-settlement-bar"><strong>预计 {pointDelta >= 0 ? "+" : ""}{pointDelta} 分</strong><span>保存后余额 {Number(profile?.points || 0) + pointDelta} 分</span><button className="primary-button" disabled={saving} type="button" onClick={onSubmit}>{saving ? "保存中…" : revision ? "修订复盘并校准" : "保存复盘并结算"}</button></section>; }
